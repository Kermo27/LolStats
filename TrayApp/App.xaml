<Application x:Class="LolStatsTracker.TrayApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:tb="http://www.hardcodet.net/taskbar"
             ShutdownMode="OnExplicitShutdown">
    
    <Application.Resources>
        <ResourceDictionary>
            <!-- Tray Icon -->
            <tb:TaskbarIcon x:Key="TrayIcon"
                           IconSource="/Assets/icon.ico"
                           ToolTipText="LoL Stats Tracker - Disconnected">
                <tb:TaskbarIcon.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Status: Disconnected" x:Name="StatusMenuItem" IsEnabled="False" FontWeight="Bold"/>
                        <Separator/>
                        <MenuItem Header="Settings..." x:Name="SettingsMenuItem"/>
                        <MenuItem Header="Open Dashboard" x:Name="DashboardMenuItem"/>
                        <Separator/>
                        <MenuItem Header="Exit" x:Name="ExitMenuItem"/>
                    </ContextMenu>
                </tb:TaskbarIcon.ContextMenu>
            </tb:TaskbarIcon>
        </ResourceDictionary>
    </Application.Resources>
    
</Application>
