<Window x:Class="LolStatsTracker.TrayApp.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LoL Stats Tracker - Settings" 
        Height="350" 
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- API Base URL -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <Label Content="API Base URL:" FontWeight="Bold"/>
            <TextBox x:Name="ApiUrlTextBox" 
                     Height="30" 
                     VerticalContentAlignment="Center"
                     ToolTip="URL of your LolStatsTracker API (e.g., http://localhost:5109)"/>
        </StackPanel>
        
        <!-- Auto Start -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <CheckBox x:Name="AutoStartCheckBox" 
                      Content="Start with Windows" 
                      VerticalContentAlignment="Center"
                      IsEnabled="False"
                      ToolTip="Automatically start the app when Windows starts (feature coming soon)"/>
        </StackPanel>
        
        <!-- Info Panel -->
        <Border Grid.Row="2" 
                Background="#F0F0F0" 
                Padding="10" 
                CornerRadius="5"
                Margin="0,0,0,15">
            <StackPanel>
                <TextBlock TextWrapping="Wrap" FontSize="11">
                    <Run FontWeight="Bold">How it works:</Run>
                    <LineBreak/>
                    • Login is required (see login window on startup)
                    <LineBreak/>
                    • Your profile is auto-created based on your Riot account
                    <LineBreak/>
                    • Ranked matches are synced automatically when you finish a game
                    <LineBreak/>
                    • Make sure your API server is running
                </TextBlock>
            </StackPanel>
        </Border>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button x:Name="TestConnectionButton" 
                    Content="Test Connection" 
                    Width="120" 
                    Height="35" 
                    Margin="0,0,10,0"
                    Click="TestConnectionButton_Click"/>
            <Button x:Name="SaveButton" 
                    Content="Save" 
                    Width="100" 
                    Height="35" 
                    Margin="0,0,10,0"
                    Click="SaveButton_Click"
                    IsDefault="True"/>
            <Button x:Name="CancelButton" 
                    Content="Cancel" 
                    Width="100" 
                    Height="35"
                    Click="CancelButton_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
